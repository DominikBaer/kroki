# Nutze ein schlankes Python-Image
FROM python:3.11-slim

# Arbeitsverzeichnis im Container festlegen
WORKDIR /app

# Kopiere die Anforderungsliste und installiere Flask
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Kopiere den gesamten Projektcode in den Container
COPY . .

# Erstelle den Upload-Ordner explizit
RUN mkdir -p uploads

# Flask-Port freigeben
EXPOSE 4444

# Starte die App (host=0.0.0.0 ist wichtig für Docker-Zugriff)
CMD ["python", "app.py"]

